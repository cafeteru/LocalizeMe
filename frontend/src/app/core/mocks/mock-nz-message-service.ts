import { NzMessageDataOptions, NzMessageRef } from 'ng-zorro-antd/message/typings';

export const mockNzMessageService = {
    create(content: string, options?: NzMessageDataOptions): NzMessageRef {
        return undefined;
    },
};
