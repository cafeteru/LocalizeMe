name: Backend Actions
run-name: ${{ github.actor }} is checking backend 🚀
on: [push]
jobs:
  lint-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://cafeteru/localize-me-backend:latest
      -  working-directory: backend
         run: golangci-lint run -v

